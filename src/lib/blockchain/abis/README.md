# Blockchain ABIs (VOTAR-385)

Generated by the blockchain repo:

```bash
cd ../blockchain && npm run export:abis
```

`BallotContract.json` is the full on-chain interface. The curated subset used at runtime lives in `src/features/voto/crypto/ballot-abi.ts` and must stay aligned with `castSignedVote`.
